/**
 * projects-section.tsx
 * ---------------------
 * Thin wrapper that keeps the existing named export `ProjectsSection` intact
 * so app/page.tsx requires no changes.
 *
 * All implementation lives in components/SurfCarousel.tsx.
 */
export { SurfCarousel as ProjectsSection } from './SurfCarousel'
